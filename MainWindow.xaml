<Window x:Class="TaskMate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskMate.ViewModels"
        mc:Ignorable="d"
        Title="To_Duo" Height="450" Width="800">
        <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#EEEEEE" Offset="0"/>
            <GradientStop Color="#D0D0D0" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
        <Window.Resources>
    <local:BoolToTextDecorationsConverter x:Key="BoolToTextDecorationsConverter" />
    </Window.Resources> 
    <Grid Margin="10">
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/> <!-- Task Input Row -->
        <RowDefinition Height="*"/>    <!-- Task List Row -->
    </Grid.RowDefinitions>  


    <!-- 📝 Task Input Section -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" VerticalAlignment="Top" HorizontalAlignment="Center">
    <StackPanel.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>
    </StackPanel.Resources>

    <TextBlock Text="Title:" />
    <TextBox Width="150" Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Due Date:" />
    <DatePicker SelectedDate="{Binding NewTaskDueDate}" Width="130"/>

    <TextBlock Text="Category:" />
    <ComboBox Width="100" ItemsSource="{Binding Categories}" SelectedItem="{Binding NewTaskCategory}" />

    <TextBlock Text="Assignee:" />
    <ComboBox Width="100" SelectedItem="{Binding NewTaskAssignee}">
        <ComboBoxItem Content="Me" />
        <ComboBoxItem Content="Partner" />
    </ComboBox>

    <Button Content="Add Task" Command="{Binding AddTaskCommand}" Width="80" Margin="10,0,0,0"/>
</StackPanel>


    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <!-- 🧍 Your To-Do List -->
    <StackPanel Grid.Column="0" Margin="10">
        <TextBlock Text="Your To-Do List" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />
        <ListView ItemsSource="{Binding Tasks}" Margin="0,0,0,10">
            <ListView.ItemContainerStyle>
    <Style TargetType="ListViewItem">
        <!-- Visibility Binding to Filter "Me" or "Partner" -->
        <Setter Property="Visibility" 
                Value="{Binding AssignedTo, Converter={StaticResource AssigneeToVisibilityConverter}, ConverterParameter=Me}" />

        <!-- Add Foreground Color Triggers -->
        <Style.Triggers>
            <DataTrigger Binding="{Binding AssignedTo}" Value="Me">
                <Setter Property="Foreground" Value="Green" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="✔" Width="30">
                        <GridViewColumn.CellTemplate>
                         <DataTemplate>
                            <CheckBox IsChecked="{Binding IsCompleted}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                    <GridViewColumn Header="Task" Width="150">
                        <GridViewColumn.CellTemplate>
                        <DataTemplate>
                         <TextBlock Text="{Binding Title}"
                                TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToTextDecorationsConverter}}" />
                         </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Due" DisplayMemberBinding="{Binding DueDate, StringFormat={}{0:MM/dd}}" Width="80"/>
                    <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
    </Grid>

    <!-- 👩‍🦰 Partner To-Do List -->
    <StackPanel Grid.Column="1" Margin="10">
        <TextBlock Text="Partner's To-Do List" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />
        <ListView ItemsSource="{Binding Tasks}" Margin="0,0,0,10">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                <!-- Visibility Binding to Filter "Me" or "Partner" -->
                    <Setter Property="Visibility" 
                        Value="{Binding AssignedTo, Converter={StaticResource AssigneeToVisibilityConverter}, ConverterParameter=Partner}" />

                <!-- Add Foreground Color Triggers -->
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding AssignedTo}" Value="Partner">
                            <Setter Property="Foreground" Value="Purple" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
        </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="✔" Width="30">
                        <GridViewColumn.CellTemplate>
                         <DataTemplate>
                            <CheckBox IsChecked="{Binding IsCompleted}" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
                    <GridViewColumn Header="Task" Width="150">
                        <GridViewColumn.CellTemplate>
                        <DataTemplate>
                         <TextBlock Text="{Binding Title}"
                                TextDecorations="{Binding IsCompleted, Converter={StaticResource BoolToTextDecorationsConverter}}" />
                         </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Due" DisplayMemberBinding="{Binding DueDate, StringFormat={}{0:MM/dd}}" Width="80"/>
                    <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</Grid>


    
</Window>
